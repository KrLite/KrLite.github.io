---
import IndexLayout from '@/layouts/IndexLayout.astro'
import { SITE } from '@/config'
---

<IndexLayout title={`404 - ${SITE.title}`} description="Not Found">
  <style>
    .error-container {
      color: var(--text-secondary);
    }
  </style>
  <p class="error-container">Page Not Found...</p>
</IndexLayout>
